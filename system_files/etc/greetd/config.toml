[terminal]
# The VT to run the greeter on
vt = 1

[default_session]
# Launch dank-greeter using a minimal Wayland compositor wrapper (niri or cage)
command = "niri --kiosk -- dank-greeter"
user = "greeter"